describe('POST create driver', () => {
    it('should create driver correctly', () => {
        expect(true).toBe(true);
    });

    it(`shouldn't create driver when incorrect data is passed`, () => {
        expect(true).toBe(true);
    });
})

describe('POST update driver', () => {
    it('should update driver correctly', () => {
        expect(true).toBe(true);
    });

    it(`shouldn't update driver when no data passed`, () => {
        expect(true).toBe(true);
    });

    it(`shouldn't update driver when no auth token passed`, () => {
        expect(true).toBe(true);
    });
})

describe('GET get driver by user id', () => {
    it('should receive driver with users auth token', () => {
        expect(true).toBe(true);
    });
})

describe('POST get driver by driver id', () => {
    it('should receive driver when pass driver id', () => {
        expect(true).toBe(true);
    });
})
